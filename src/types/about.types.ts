// src/types/aboutTypes.ts
export interface AboutQuote {
  quoteText: string;
  authorName: string;
  authorTitle: string;
}

export interface AboutContent {
  title: string;
  description: string;
}
